# zxyh

```
	git add .
	git commit -m "a"
	git remote add name https://github.com/XinHuaLuFang/zxyh.git
	git push -u name master
```

- - -

```
	git add .
	git commit -m "a"
	git push -u name master
```